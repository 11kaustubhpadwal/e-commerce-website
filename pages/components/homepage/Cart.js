import React from "react";
import { Grid, Segment } from "semantic-ui-react";

const Cart = () => {
  return (
    <Grid padded>
      <Grid.Row>
        <p>Your cart is currently empty.</p>
      </Grid.Row>
    </Grid>
  );
};

export default Cart;
